# DigiShell Server Configuration
# Copy this file to .env and customize as needed

# Server bind address
# 0.0.0.0 = all network interfaces (LAN access)
# 127.0.0.1 = localhost only (most secure)
DIGISHELL_HOST=0.0.0.0

# Server port
DIGISHELL_PORT=8000

# Usage:
# 1. Copy this file: cp .env.example .env (Unix) or copy .env.example .env (Windows)
# 2. Edit .env with your preferred settings
# 3. Run DigiShell - it will automatically load these settings
